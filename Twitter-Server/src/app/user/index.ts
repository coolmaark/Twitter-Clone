import { types } from "./types";
import { queries } from "./queries";
import { resolvers } from "./resolvers";
import { muatations } from "./mutations";
export const User = { types, queries, resolvers, muatations };
